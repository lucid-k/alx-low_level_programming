#include "main.h"
#include <stdio.h>

/**
 * main - function definition
 * Definition - add 2 numbers
 * Return: result of addition.
 */
int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}
